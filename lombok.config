lombok.copyableAnnotations += org.jdbi.v3.json.Json
config.stopBubbling = true
# Clear the list first to ensure no Jackson 2 defaults are lingering
clear lombok.jacksonized.jacksonVersion
# Add Jackson 3 to the list using the correct += syntax
lombok.jacksonized.jacksonVersion += 2
lombok.anyConstructor.addConstructorProperties=true