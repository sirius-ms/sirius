dependencies {
    api project(':fragmentation_tree:fragmentation_tree_construction')
    // intellij seems to ignore other scopes than compile so we need to have this here and remove it before distribution
    implementation "com.gurobi:gurobi-jar:$gurobi_version"
}
