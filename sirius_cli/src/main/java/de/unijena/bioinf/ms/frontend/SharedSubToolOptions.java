package de.unijena.bioinf.ms.frontend;

public class SharedSubToolOptions {
   /* @Option(names = "--recompute", description = "Recompute ALL results of this SubTool if already present. By default already present results will be preserved and the instance will be skipped", order = 95)
    public boolean recompute;*/
}
